sig hidden.

kind    i, list          type.

type    nill             list.
type    cons             i -> list -> list.

type    rev1, rev2       list -> list -> o.
type    aux1             list -> list -> list -> o.
