sig lists.

kind    i, list       type.

type    nl            list.
type    cons          i -> list -> list.

type    list          list -> o.
type    append        list -> list -> list -> o.
type    rev, perm     list -> list -> o.
type    select        list -> i -> list -> o.
