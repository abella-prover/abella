(executable
  (name abella)
  (modules abella)
  (libraries abella_lib unix))

(library
  (name abella_lib)
  (modules :standard \ abella)
  (wrapped false))

(ocamllex
  (modules lexer))

(ocamlyacc
  (modules parser))