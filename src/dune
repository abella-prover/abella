(executable
  (name abella)
  (modules abella)
  (libraries src unix))

(library
  (name src)
  (modules :standard \ abella)
  (wrapped false))

(ocamllex
  (modules lexer))

(ocamlyacc
  (modules parser))